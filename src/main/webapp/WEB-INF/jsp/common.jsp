<%@ page language="java" pageEncoding="UTF-8"%>
<%
/* String path = request.getContextPath();
Boolean isSSL = request.getHeader("X-ssl") != null && request.getHeader("X-ssl").equals("true");
String basePath = isSSL ? ("https://" + request.getServerName() + "/") : (request.getScheme() + "://" + request.getServerName() + ":" + request.getServerPort() + path + "/");
request.setAttribute("path", path);
request.setAttribute("basePath", basePath);
request.setAttribute("x_ssl", request.getHeader("X-ssl")); */
%>
